# Agent AI — Продвинутый AI-агент с самомодификацией

**Agent AI** — это мощная экосистема автономного агента, способного не только общаться, но и управлять операционной системой, искать информацию в сети, выполнять сложный код и динамически расширять свою функциональность.

## 🚀 Основные возможности

*   **🖥️ Computer Use:** Полное управление рабочим столом Windows (движение мыши, ввод текста через буфер обмена, горячие клавиши, скролл). Агент "видит" экран и принимает решения на основе визуального анализа.
*   **🌐 Веб-интерфейс:** Современный и быстрый UI (Vue.js, Tailwind), работающий через локальный сервер. Поддерживает стриминг ответов (SSE), подсветку кода, LaTeX, динамический фон и управление историей чатов.
*   **🔌 Плагинная архитектура:** Система плагинов позволяет добавлять новые возможности без вмешательства в ядро. Каждый плагин может внедрять свои инструменты, промпты и логику инициализации.
*   **🧠 Процесс мышления:** Визуализация цепочки рассуждений (Chain of Thought), позволяющая видеть, как агент анализирует задачу перед выполнением.
*   **🛠️ Самомодификация:** Агент обладает способностью редактировать собственный код. Изменения сохраняются в `saved_code_changes.py` и автоматически применяются при каждом запуске.
*   **📦 Sandbox:** Встроенный инструмент для создания изолированных копий проекта. Позволяет безопасно тестировать новые функции или изменения, не рискуя стабильностью основной системы.
*   **🔑 Мульти-ключи:** Автоматическая ротация API-ключей Gemini при достижении лимитов (Rate Limits) для обеспечения бесперебойной работы.
*   **🖼️ Мультимодальность:** Поддержка работы с изображениями — агент может анализировать скриншоты или фотографии, загруженные в чат.

## 📁 Структура проекта

*   `agent.py` — Ядро системы, содержащее базовую логику класса `Chat` и механизмы работы с API.
*   `start.py` — Главная точка входа. Загружает конфигурацию, инициализирует плагины и собирает системный контекст.
*   `plugins/` — Каталог расширений:
    *   `web_interface/` — Полнофункциональный веб-интерфейс и локальный HTTP-сервер.
    *   `computer_use/` — Набор инструментов для управления ОС и взаимодействия с GUI.
*   `keys/` — Директория для API ключей (Gemini, Google Search).
*   `chats/` — Хранилище истории диалогов и состояний Python-окружения (PKL/JSON).
*   `prompts/` — Текстовые инструкции для системы и каждого отдельного инструмента.
*   `user_profile.json` — Долговременная память агента о пользователе (предпочтения, навыки, история).

## 🛠 Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/ilia202015/AgentAI.git
    cd AgentAI
    ```

2.  **Установите необходимые библиотеки:**
    ```bash
    pip install -r requirements.txt
    ```

3.  **Настройте API ключи:**
    *   Создайте папку `keys/`.
    *   Положите ваш ключ Gemini в файл `keys/gemini0.key`. Можно добавить дополнительные ключи (`gemini1.key`, `gemini2.key`...), агент будет переключаться между ними автоматически.
    *   Для работы поиска (инструмент `google_search`) создайте `keys/google.key` (API Key) и `keys/search_engine.id` (CSE ID).

4.  **Запустите Агента:**
    ```bash
    python start.py
    ```
    По умолчанию агент запустит веб-интерфейс. Откройте `http://127.0.0.1:8080` в браузере.

## ⚙️ Плагины и инструменты

### Web Interface
Основной способ взаимодействия с агентом.
*   **Выбор моделей:** Поддержка последних моделей Gemini (Flash, Pro).
*   **Интерактивность:** Кнопки действий, прогресс-бары и кастомные HTML-виджеты прямо в чате.
*   **Управление контекстом:** Возможность очистить Python-окружение чата, сохранив при этом историю сообщений.

### Computer Use
Позволяет делегировать агенту рутинные задачи на компьютере.
*   **Индикация:** При работе Computer Use отображается специальный оверлей, информирующий о действиях агента.
*   **Безопасность:** Мониторинг вмешательства пользователя — агент приостановит работу, если вы начнете двигать мышью.
*   **Интеграция:** После выполнения задачи агент автоматически возвращает фокус на окно чата.

---
*Проект находится в активной разработке. Агент постоянно обучается и улучшает свои возможности.*
