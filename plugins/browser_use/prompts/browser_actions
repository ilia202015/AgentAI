Выполнить пакет команд. Основной инструмент навигации.
ТИПЫ КОМАНД:
- Навигация: `click` (по id), `type` (ввод текста), `scroll`, `wait` (мс), `hover` (наведение).
- Данные: `get_text` (получить полный текст элемента без обрезки).
- Вкладки: `list_tabs` (вернет список id и title), `switch_tab` (переключить фокус), `close_tab`.
- JS: `js_exec` (выполнить код в контексте страницы, вернуть результат).

ОГРАНИЧЕНИЯ ВОСПРИЯТИЯ (учитывай это при поиске):
- Текст элементов в `get_state` обрезан до 50 символов.
- ID и ClassName обрезаны до 40 символов.
- SVG иконки заменены на `[Icon: label]`.
- IFrame заменены на плейсхолдеры.

СОВЕТ: Сначала всегда делай `get_state`, чтобы увидеть актуальные ID элементов.